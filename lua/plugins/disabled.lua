return {
  {
    "echasnovski/mini.ai",
    enabled = false,
  },
  -- {
  --   -- This can be in any file that gets loaded after plugin initialization
  --   "Saghen/blink.cmp",
  --   config = function()
  --     -- Add this to run after the plugin is loaded
  --     vim.api.nvim_create_autocmd("VimEnter", {
  --       callback = function()
  --         vim.cmd("iunmap <CR>")
  --       end,
  --       once = true,
  --     })
  --   end,
  -- },
}
